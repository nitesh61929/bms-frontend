import { Logger } from "../adapters/logger";
import { SansDependencies } from "../binding";
import { Converter } from "../converters/types";
import { findOriginalConfigurations } from "../input/findOriginalConfigurations";
import { ResultStatus } from "../types";
export declare type RunCliDependencies = {
    converters: Converter[];
    findOriginalConfigurations: SansDependencies<typeof findOriginalConfigurations>;
    logger: Logger;
};
/**
 * @see `/docs/Architecture/README.md` for documentation.
 */
export declare const runCli: (dependencies: RunCliDependencies, rawArgv: string[]) => Promise<ResultStatus>;
