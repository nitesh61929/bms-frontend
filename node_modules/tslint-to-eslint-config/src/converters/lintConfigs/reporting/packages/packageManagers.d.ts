export declare enum PackageManager {
    npm = 0,
    pnpm = 1,
    Yarn = 2
}
export declare const preferredLockfiles: Map<PackageManager, string>;
export declare const installationMessages: {
    0: (packages: string) => string;
    1: (packages: string) => string;
    2: (packages: string) => string;
};
