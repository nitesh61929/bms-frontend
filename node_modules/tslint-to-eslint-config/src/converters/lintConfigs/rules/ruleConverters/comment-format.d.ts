import { RuleConverter } from "../ruleConverter";
export declare const CapitalizedIgnoreMessage = "Only accepts a single string pattern to be ignored.";
export declare const convertCommentFormat: RuleConverter;
