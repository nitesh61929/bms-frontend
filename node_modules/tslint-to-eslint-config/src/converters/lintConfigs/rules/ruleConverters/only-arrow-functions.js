"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertOnlyArrowFunctions = void 0;
const convertOnlyArrowFunctions = (tslintRule) => {
    const notices = [];
    if (tslintRule.ruleArguments.includes("allow-declarations")) {
        notices.push("ESLint does not support allowing standalone function declarations.");
    }
    if (tslintRule.ruleArguments.includes("allow-named-functions")) {
        notices.push("ESLint does not support allowing named functions defined with the function keyword.");
    }
    return {
        rules: [
            {
                ...(notices.length !== 0 && { notices }),
                ruleName: "prefer-arrow/prefer-arrow-functions",
            },
        ],
        plugins: ["eslint-plugin-prefer-arrow"],
    };
};
exports.convertOnlyArrowFunctions = convertOnlyArrowFunctions;
//# sourceMappingURL=only-arrow-functions.js.map