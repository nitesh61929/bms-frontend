import { ConversionError } from "../../../errors/conversionError";
export declare const createStubConverter: (result: ConversionError | string[]) => () => ConversionError | {
    rules: {
        ruleName: string;
    }[];
};
