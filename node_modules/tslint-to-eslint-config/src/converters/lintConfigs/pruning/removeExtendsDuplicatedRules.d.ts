import { ESLintRuleOptions, ESLintRuleOptionsWithArguments } from "../rules/types";
/**
 * Finds only the ESLint rules configured differently than their (extended) configurations.
 */
export declare const removeExtendsDuplicatedRules: (userRules: Map<string, ESLintRuleOptions>, extensionRules: Map<string, ESLintRuleOptionsWithArguments>) => {
    differentRules: Map<string, ESLintRuleOptions>;
    extensionRules: Map<string, ESLintRuleOptionsWithArguments>;
};
