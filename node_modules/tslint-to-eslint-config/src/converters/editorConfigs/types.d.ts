import { TSLintToESLintSettings } from "../../types";
export declare type EditorConfigConverter = (rawEditorSettings: string, settings: TSLintToESLintSettings) => EditorConfigConversionResults;
export declare type EditorConfigConversionResults = {
    contents: string;
    missing: string[];
};
export declare type EditorConfigsConversionResults = {
    failed: Map<string, Error>;
    successes: Map<string, EditorConfigConversionResults>;
};
export declare type EditorConfigDescriptor = readonly [string, EditorConfigConverter];
