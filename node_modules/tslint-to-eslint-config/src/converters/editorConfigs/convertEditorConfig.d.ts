import { FileSystem } from "../../adapters/fileSystem";
import { TSLintToESLintSettings } from "../../types";
import { EditorConfigConverter } from "./types";
export declare type ConvertEditorConfigDependencies = {
    fileSystem: Pick<FileSystem, "readFile" | "writeFile">;
};
/**
 * @see /docs/Editors.md for documentation.
 */
export declare const convertEditorConfig: (dependencies: ConvertEditorConfigDependencies, converter: EditorConfigConverter, requestedPath: string, settings: TSLintToESLintSettings) => Promise<Error | import("./types").EditorConfigConversionResults>;
