import { GlobAsync } from "../../adapters/globAsync";
import { CommentFileNames } from "../../comments/collectCommentFileNames";
import { ResultWithDataStatus } from "../../types";
export declare type ExtractGlobPathsDependencies = {
    globAsync: GlobAsync;
};
export declare const extractGlobPaths: (dependencies: ExtractGlobPathsDependencies, { exclude, include }: CommentFileNames) => Promise<ResultWithDataStatus<string[]>>;
