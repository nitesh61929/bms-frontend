import { SansDependencies } from "../binding";
import { importer } from "./importer";
export declare const findRawConfiguration: <Configuration>(fileImporter: SansDependencies<typeof importer>, filePath: string, defaults?: Partial<Configuration>) => Promise<Error | Configuration>;
